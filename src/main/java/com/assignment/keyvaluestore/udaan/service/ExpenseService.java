package com.assignment.keyvaluestore.udaan.service;

import org.springframework.stereotype.Service;

@Service
public class ExpenseService {

    void settlement(){
        // will record the transaction and update the pending amount
    }

    void addExpenses() {
        // define total amount & also update the pending amount
        // will have logic to split the money percentage wise or amount wise
    }

}
