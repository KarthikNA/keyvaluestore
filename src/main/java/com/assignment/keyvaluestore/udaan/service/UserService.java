package com.assignment.keyvaluestore.udaan.service;

import com.assignment.keyvaluestore.udaan.model.PendingAmount;
import com.assignment.keyvaluestore.udaan.model.User;
import org.springframework.stereotype.Service;

@Service
public class UserService {

    public User createUser(User user) {
        // creates user in the system
        return  null;
    }

    public PendingAmount findPendingAmount() {
        // will return the amount the user owes and the amount the user needs to receive from other users
    }

}
